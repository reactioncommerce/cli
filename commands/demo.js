import Logger from "../utils/logger.js";

/**
 * @summary download demo files and runs docker-compose
 * @returns {undefined} undefined
 */
export default function demo() {
  Logger.info("running demo command");
}
