import Logger from "../utils/logger.js";

/**
 * @summary start core in serve (no watch, no restart) mode
 * @returns {undefined} undefined
 */
export default function run() {
  Logger.info("running core");
}
